<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fariz.cariuntung">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:largeHeap="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">



        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <activity android:name=".Login"/>
        <activity
            android:name=".Maps"
            android:label="@string/title_activity_maps" />
        <activity android:name=".profil" />
        <activity android:name=".registrasi" />
        <activity android:name=".updateprofil" />
        <activity android:name=".Cari_Tempat" />
        <activity android:name=".about" />
        <activity android:name=".submenu_detaildata" />
        <activity android:name=".Menuutama" />
        <activity android:name=".CustomerList" />
        <activity android:name=".customer" />
        <activity android:name=".LokasiList" />
        <activity android:name=".LokasiHasil" />
        <activity android:name=".LokasiList2" />
        <activity android:name=".lokasi" />
        <activity android:name=".penilaiandetail" />
        <activity android:name=".penilaiandetailList" />
        <activity android:name=".penilaian" />
        <activity android:name=".PenilaianList" />
        <activity android:name=".Activity_Pertanyaan" />
        <activity android:name=".LokasiListParameter" />
        <activity android:name=".listParameter" />
        <activity android:name=".LokasiFilter" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>